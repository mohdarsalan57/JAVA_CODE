public class tern {
    public static void main(String args [])
    { //ternary operator use
        int n=5;
        int result =0;
        //  result = n%2==0 ?10 :20;
        //this is if else and above use of ternary in comment
        if (n%2==0)
        result=10;
        else
        result=20;
        System.out.println(result);
    }
}
